# 技術債追蹤

> 彙整架構優化各階段(1-6)與 issue #68 處理(2026-07-04,`issue-68-quality` 分支)審查過程中發現、但當時判斷「非阻擋項、可留待後續」而未處理的問題。每項附出處與最近一次驗證的現狀。若之後處理掉,請把對應項目移到「已處理」並註明是哪個階段/commit。

## 待處理

### 3. `medicationConfig.jsx` / `labConfig.jsx` 的 `elementButton`/`customTextField` 重複

- **出處**:`docs/superpowers/plans/2026-07-03-refactor-phase5.md`「審查累積追蹤項目」。
- **現狀(已驗證,仍成立)**:`src/components/tabs/copyFormat/medicationConfig.jsx`(184、204 行)與 `src/components/tabs/copyFormat/labConfig.jsx`(166、186 行)各自定義幾乎相同的 `elementButton(item, onClick)` 與 `customTextField({...})` helper(約 60 行重複),未抽成共用 util。屬選配項,非阻擋。

### 4. `NHITW_DATA`(跨擴充功能交換契約)截斷策略未定案(監控已完成)

- **出處**:`docs/superpowers/2026-07-02-refactor-handoff.md`「階段 7+ 候選項目」。
- **現狀(2026-07-05 更新,`techdebt-4-data-size-monitor` 分支)**:**最小版監控已完成**——
  `writeShareDataToLocalStorage` 寫入前量測序列化大小(UTF-16 每字元估 2 bytes),超過
  `NHITW_DATA_WARN_BYTES`(4MB)經 `debugLog` 告警;寫入失敗的 `console.error` 附估算大小;
  `buildShareData` 新增契約欄位 `truncated`(目前恆 `false`,DOC/02 已同步)。
- **殘留**:超限時的**截斷策略**(哪些資料型別可捨、捨棄順序)涉及臨床資料取捨,
  需維護者臨床判斷後才實作;實作時由截斷邏輯把 `truncated` 設為 `true`。
  在那之前超限只警告、照常嘗試寫入。

### 5. 23 個既存 lint warnings(errors 已於 issue #68 清零)

- **出處**:issue #68 第 1 點處理(2026-07-04,`issue-68-quality` 分支)後的殘留。
- **現狀**:`npm run lint` = **0 errors + 23 warnings**。warnings 組成:
  `react-refresh/only-export-components` 15 個(dev HMR 品質,不影響產品)、
  `react-hooks/exhaustive-deps` 8 個(**修復會改變 effect 觸發時機,屬行為變更**,
  依「先測試後重構」紀律需先有對應行為測試才能動,故另案)。
  維護標準:errors 維持 0(CI 強制),warnings 不新增。

### 5a. 時區敏感測試:`calculateRemainingDays`

- **出處**:issue #68 的 CI gate 首跑即在 UTC runner 上抓到(2026-07-04)。
- **現狀**:`chineseMedProcessor.calculateRemainingDays` 以本地時區計算午夜界線,
  `tests/test_chineseMedProcessor.js` 對應測試在 `TZ=UTC` 下失敗(可本地重現)。
  目前以 CI `env: TZ: Asia/Taipei` 明訂前提繞過;根本解法是讓計算以明確時區(+0800)
  而非執行環境時區為準,屬行為敏感變更,需先補時區矩陣測試再動。

### 5b. Chrome 109 runtime 相容性未驗證

- **出處**:issue #68 第 2 點(danny0838)。
- **現狀**:語法層已由 build target `chrome109` 保證(vite + scripts/build.js 三處對齊),
  manifest 已宣告 `minimum_chrome_version: "109"`;但 React 19 + MUI 6 + @mui/x-charts 8
  在 109 引擎的 runtime 行為未實測。一次性驗證指南(含已驗證的 Chromium 109 下載來源與
  checklist)在維護者本機 `.test_data/chrome109-testing.md`。驗過一次即可結案。

### 6. `FloatingIcon`/`useNhiDataState` 內 `handleData` 對 `userInfo` 的既存 stale read

- **出處**:`docs/superpowers/2026-07-02-refactor-handoff.md`「審查累積的既存問題」#6。
- **現狀(依現行程式碼結構推斷,問題性質不變)**:`src/hooks/useNhiDataState.js` 的 `useEffect(() => { handleData(); }, [labSettings])` 依賴陣列只列 `labSettings`,`handleData` 內部讀取的 `userInfo`/`setUserInfo` 來自 hook 參數(每次 render 重新建立的閉包),但此 effect 只在掛載或 `labSettings` 變動時重跑。掛載當下 `userInfo` 通常仍是 `useUserInfo` 初始值(尚未由 JWT/本地匯入解析完成),故此路徑上的比較(`window._localUserInfo` 分支)可能讀到「稍舊」的 `userInfo` 快照。**性質同交班文件原始判斷:僅可能造成多餘一次 re-render,不影響最終顯示正確性**(因為 `!userInfo || ...` 的判斷式在 `userInfo` 為初始值/null 時仍會正確觸發 `setUserInfo`)。經由 `nhiDataRef.current.handleData()` 這條路徑(`useSettingsState` 的非同步 listener 觸發)因階段 4 引入的 ref 模式,已能讀到最新 `userInfo`,不受此問題影響。

## 記錄以避免誤判(非技術債,設計決策)

### `settingsManager.js` 的 `settingTypeHandlers` 無 `'western'` 條目

- **出處**:`docs/superpowers/plans/2026-07-03-refactor-phase5.md`「審查累積追蹤項目」。
- **現狀(2026-07-04 重驗,仍成立,確認為既存設計而非漏補)**:`src/utils/settingsManager.js#handleDataFetchCompletedSettingsChange`(issue #68 重構後檔案已縮至約 350 行,Map 位置改變但設計不變)的 `settingTypeHandlers` Map 只有 `'chinesemed'`/`'labsettings'`/`'overview'`/`'generalDisplay'`/`'cloud'` 五個 key,沒有 `'western'`。西藥(medication)設定變更改走 `chrome.storage.onChanged` 事件傳播(`listenForSettingsChanges`,由 `useSettingsState.js` 的另一個 listener 處理,非 `dataFetchCompleted` 事件的 `settingsChanged` 分支),兩者是平行的兩條設定變更通知路徑。**這是既存設計,不是遺漏**,記錄此條是為了避免後續審查誤判為「漏補西藥分支」而重複開票。

## 已處理(供對照,曾在追蹤清單但已解決)

以下項目原列於審查累積清單,已在對應階段修復,列出供歷史對照(不需再行動):

| 項目 | 修復階段 |
|---|---|
| `LabData.jsx`「複製全部」引用不存在的 `setSnackbarMessage`/`setSnackbarOpen`(no-undef,ReferenceError) | 階段 5 |
| `Overview_AdultHealthCheck.jsx`/`Overview_CancerScreening.jsx`/`Overview_hbcvdata.jsx` 死碼 | 階段 5 |
| `LineSpacingWrapper.jsx` 死碼(fallback 引用不存在的 `lineSpacingHeight`) | 階段 5 |
| 病患切換 debug log 印出完整身分證號 | 階段 6(`maskPatientId()`,見 `src/apiInterceptor/index.js`) |
| processor 輸出無型別描述 | 階段 6(`src/types/processors.js` + `npm run type-check`) |
| `legacyContent.js` 名不符實 | 階段 6(改名/拆解為 `src/apiInterceptor/`) |
| `CKMSummaryBar` 尚未遷移到 `SettingsContext`(原本項 #2) | 2026-07-05(`techdebt-2-ckm-summarybar-context`):改用 `useGeneralDisplaySettings()`,兩呼叫端移除 `gds` prop;prop drilling 例外清零。行為測試 `tests/CKMSummaryBar.test.jsx` 先紅後綠 |
| `userInfoUtils.js` 與 `ageUtils.js` 年齡計算重複(原本項 #1) | 2026-07-05(`techdebt-1-age-consolidation`):先補兩路徑一致性 characterization 測試(`tests/userInfoUtils.test.js`,4 個日期 × 5 個生日矩陣)綠後,`extractUserInfoFromToken` 改呼叫 `calculateAgeFromROCBirthday` 單一來源。**含一項行為修正**:7 碼非數字垃圾生日 age 由 `NaN`(會顯示「NaN」)改為 `null` |
| ~1,355 個既存 lint 問題(原本項 #5) | issue #68(2026-07-04,`issue-68-quality`):規則校準 + `--fix` + 手動清理 + 死碼移除約 475 行,errors 歸零並由 CI 強制;殘留 23 warnings 見新版 #5 |
| 設定值散落多處(新增一個設定要改 5–7 檔) | issue #68:`src/config/settingsSchema.js` 單一事實來源,`settingsManager` 531→351 行;新流程見 `Settings.md` |
| 設定 UI 元件硬寫預設值與 `defaultSettings.js` 矛盾(useColorfulTabs/floatingIconPosition/字級) | issue #68(bug fix,影響未存過設定者的初始顯示) |
| 無 CI(lint/test/build 靠人工自律) | issue #68:`.github/workflows/ci.yml` |
| manifest 未宣告 `minimum_chrome_version`、build 無明確 target | issue #68:109 + `chrome109`(三處對齊) |
| `package-lock.json` 在 .gitignore,建置不可重現(原 #5c) | issue #68 後續(2026-07-04,維護者核准):lockfile 入版控,CI 改回 `npm ci` + cache |

## 附註:驗證過程中發現的極小項目(未列入正式追蹤,供參考)

- ~~`src/utils/userInfoUtils.js` 的 `getRawToken` 未使用 import~~ — 已於 issue #68 lint 清理移除。
